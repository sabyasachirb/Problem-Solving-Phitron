#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false); cin.tie(NULL);

    int t;
    cin >> t;
    while(t--)
    {
        int n;
        cin>>n;
        vector<pair<int, int>> watch;
        for(int i=1; i<=n; i++)
        {
            int x;
            cin>>x;
            watch.push_back({x,i});
        }
        long long ans = 0;
        for(int i=0; i<n)

    }
    return 0;
}