#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while(t--)
    {
        int n;
        cin >> n;
        deque<int> a;
        for(int i=1; i<n; i++)
        {
            int x;
            cin >> x;
            a.push_back(x);
        }
    }
    return 0;
}